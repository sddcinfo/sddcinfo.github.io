---
// Override for Starlight's page footer: render nothing.
//
// The default footer chrome (prev/next pagination, last-updated, edit-link, and
// the site footer line) is noise on a generated, line-cited code wiki, so this
// component intentionally emits no markup. Wired via `components.Footer` in
// astro.config.mjs.
---
